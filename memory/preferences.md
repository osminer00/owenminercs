# Preferences

Last reviewed: 2026-07-10

## Durable User Preferences

- **Hosting:** The live site is **GitHub + Cloudflare** (not Netlify). Do not tell the user to wait for or check Netlify deploys.
- User wants a real cross-platform memory layer shared by Codex, Claude, Cursor, Gemini, Antigravity, Ollama, and similar tools.
- Keep memory token-light: short canonical files, not long duplicated handoffs.
- Keep memories updated when the user gives durable site facts, bug patterns, design ratios, layout preferences, or workflow rules.
- Track issues in a memory file so future chats can diagnose recurring problems.
- Prefer a practical "growing brain" over isolated assistant-specific notes.
- On this project, prefer delegating lightweight AI work to local Ollama/GPU first when it will not slow progress; keep cloud models available for harder reasoning and final critical passes to reduce monthly hosted usage.
- **Programs page** (`dev/dev-stack.html`, nav label **Programs**): sections for coding stack (Cursor Pro+ with refer-a-friend `https://cursor.com/referral?code=AXMCFHZ8A2DG`, Codex/VS Code/VS, Claude paused, Antigravity+Gemini, Ollama), Adobe (Premiere, Photoshop, After Effects / projection mapping → Upgrades), GIMP origins, OBS + open-source multi-RTMP (GitHub). Video history on page: Windows bundled editor first → Vegas Movie Studio (Amazon, ~early teens) → YouTube phase → Premiere now.
- Cursor usage limits are driven mainly by **hosted** chat/agent models; **local Ollama inference does not replace that** unless each chat’s **model picker** is set to a **local/Ollama** model (watch GPU under **`ollama`**, not only `Cursor`). Built-in subagents may still use **automatic/fast cloud** models unless configured otherwise.
- Cross-workspace memory routing: user-level Cursor hook writes `%USERPROFILE%\.cursor\active-memory-root.txt` when the opened workspace contains `AGENTS.md` + `memory/`; global Cursor rules follow that pointer so non-site repos do not keep pulling OwenMinerCS context.

## UI / Design Preferences

- **Liquid glass theme (bubble):** **Site-wide unified Content (socials) purple theme** (2026-07-10): all bubble pages + home use socials bubble clip (`/images/bubble-themes/socials/bg.mp4`, ~2:12–2:36, poster 2:15), `bubble-theme--socials`, and `css/bubble-themes/socials.css`; home accents in `home-liquid-glass-test.css` match socials purple. Bulk refresh: `node dev/unify-bubble-socials-theme.mjs`; new pages: `node dev/apply-bubble-themes.mjs`. **Dark** Apple-style liquid glass UI. **Floating social dock** (`#site-support-dock`, `.site-social-nav--dock`, `.site-social-dock-reset`) is **excluded** from bubble/liquid-glass CSS — it keeps original `owenminercs.css` styling only. **Home** keeps `home-liquid-glass-test` + **dark green primary accent** (`/images/home/bubble-bg.mp4`, segment ~1:48–2:12, poster ~2:19). **Ping-pong loop:** bubble background MP4s are forward + reversed concat (~48s from 24s source) so native `loop` has no harsh color jump at the seam; scroll-reactive playback idle **0.5×** (`scripts/bubble-scroll.js` BASE_RATE; range 0.25–0.75× on scroll). Regenerate via `makePingPongMp4()` in `dev/extract-bubble-segments.mjs`. **Nav sections** use `body.bubble-theme bubble-theme--{section}` + `css/bubble-theme-base.css` + `css/bubble-themes/{section}.css` + `css/liquid-glass-surfaces.css` (shared glass). **Subpages inherit the parent nav section theme** (Gear/`The Setup`+`Keyboard`+`PC`+`Upgrades` → `bubble-theme--gear`; Gaming/`Counter-Strike` → `--gaming`; etc.) — apply or refresh via `node dev/apply-bubble-themes.mjs`. **Home + Gaming are the reference look** (do not regress). Liquid glass + `.home-pillars` quick links follow each section’s `--bubble-accent*` / `--bubble-glow-*` (mapped in `bubble-theme-base.css` to `--lg-accent-*` and `--site-nav-*`). Per-section bubble video tint via `--bubble-blue-block-filter` in each `css/bubble-themes/{section}.css` (background only). **Blue-blocked bubble video (background only):** `.home-bubble-bg__media` + `.bubble-bg__media` use `brightness(var(--bubble-media-brightness)) saturate(0.85) sepia(0.35) hue-rotate(85deg)` — blues/cyans read near black like blue-light glasses; home brightness `0.58`, sections `0.34` (gaming `0.32`); green-tinted dark veil scrim on top. **Not** applied to card images, logos, or social dock. **Clear center:** very low white fill (`--lg-glass-fill` ~0.015), dark tint **edge-only** via radial gradient (transparent ~54–86% center → frost/dark at 85–100% rim), `backdrop-filter brightness(0.92–0.94)`, prose readability via `--bubble-text-scrim` (~0.46 home / ~0.4 sections) not panel opacity. **No image overlays:** card photos/thumbs (`.home-explore-card__media`, `.keep-card__thumb`, YT tiles) stay full clarity — no gradient/`::after` scrim over image area; liquid glass + chroma glow on card **edges** and **text body** below image only. **Strong opaque edge rim (Apple-style, inside glass thickness):** crisp 1px `rgba(255,255,255,0.32)` border + tight neutral float shadow only — **no colored outer halo, no `drop-shadow` bloom**; edge strength via 2px `::before` conic/linear rim (opacity ~0.92, **dark evergreen → mid green → light green**), radial-masked edge `backdrop-filter` blur ~20px, `::after` bottom trapped chromatic band (opacity ~0.68, blur ~22px, clipped by `overflow:hidden`), inset `0 0 0 1px` + side/top specular + bottom inset glow. **Evergreen bias (main theme):** user favorite color is green but **not neon** — bubble accents use dark evergreens (`#0f2e1a` / `#14532d` / `#166534` / `#15803d` primary, `#059669` hover lift); edge chroma uses dark evergreen fills + `#22c55e` / `#4ade80` only at ≤0.3 opacity for edge specular — **no orange or blue/cyan in global glass chroma**. Gear/qa/achievements accents are dark teal/mint/emerald variants; gaming keeps red primary; help-wanted keeps indigo primary; donators/socials/dev/garage-sale use evergreen green primaries (tone-shifted per section). Active nav pills: white/light text on dark-green fill, not bright green on bright green. **Per-card palettes:** content cards (`.home-explore-card`, `.keep-card`) use `data-card-theme` with image-matched `--card-glow-*` — `cool-blue-purple` and `kilowatt-orange` shifted to **forest evergreen** on home; `warm-amber` (merch/warm lighting), `cyan-teal` (PC), `gold-amber` (Labubu), `brand-green` (logo/socials), `neutral-slate` (placeholders); mapping in `scripts/home-explore-carousel.js` + `css/home-liquid-glass-test.css`; section wrappers keep global evergreen edge. **Home Explore carousel previews:** each card crossfades destination gallery images (~2s hold, ~480ms fade) from fetched page galleries (photogallery, keep thumbs, thumb rows, build galleries); pauses on card hover/focus and when off-screen; `prefers-reduced-motion` keeps static thumb only. **Text contrast (bubble):** nav default `rgba(255,255,255,0.9)` (visited `0.86`, active `0.98`); card descriptions `0.82`, tags `0.74`; home pillars row `0.88`. **Corner split:** `--lg-glass-radius` 20px on main panels only (intro, section wrappers, header/footer chrome); `--lg-glass-radius-card` 0 on buttons, nav pills, cards, tiles, and form fields. Video segments (24s source → **48s ping-pong** in MP4, C0103.MP4, static tripod / no straw-hand, re-extracted 2026-06-30): **each nav section uses a distinct color window** (not clustered 0–36s). gaming 0:12–0:36 poster 0:15 (red+blue, reference); gear uses socials clip 2:12–2:36 poster 2:15 (same bubble video as Content; cyan/teal UI accents in gear.css); socials 2:12–2:36 poster 2:15 (purple+cyan); help-wanted 2:36–3:00 poster 2:39 (deep purple); donators 3:00–3:24 poster 3:03 (violet); dev 3:24–3:48 poster 3:27 (purple+teal); qa 3:30–3:54 poster 3:33 (electric blue); achievements 3:57–4:21 poster 4:00 (neon green+purple); garage-sale 4:48–5:12 poster 4:51 (red+magenta push via CSS). Avoid 1:36–2:04 (straw) and 4:24+ (metal straw). Home unchanged 1:48–2:12 poster 2:19 with green hue filter. **Section bubble CSS must not use global green `hue-rotate(85deg)`** — preserve native clip colors; home-only green filter in `home-liquid-glass-test.css`. Subpages inherit parent nav theme via `dev/apply-bubble-themes.mjs`.
- **Setup hub** (`The Setup/the-setup.html`, nav label **Gear**): `.keep-board--hub` uses **swipe carousel** thumbs via `keep-thumbs.js` (touch/drag + arrow buttons + **auto-advance ~3.2s**; pauses on hover/focus/off-screen). Category titles are **large** type placed in `.keep-card__body` directly under `.keep-card__video-slot` (not overlaid on images). Hub grid is **5 columns** on desktop (4/3/2 at breakpoints; **2-col Instagram-style tiles on phone ≤700px**). Page uses the standard centered `site-card-ui` column (~1240px) like other hub pages (e.g. Gaming)—not full-bleed viewport width. **SEO intro:** keep a one-line `.gear-hub__tagline` in the hero; move the long keyword/link paragraph to bottom `.gear-hub__seo` (still in DOM for crawlers; de-emphasized styling)—do not restore a long lede above the card grid. **Gaming hub** (`Gaming/gaming.html`) uses the same bottom pattern: `.gaming-hub__seo` after the card grid.
- **Accent colors:** Green glow/fill for non-link UI (hero H1, buttons, social tip chrome, dock fidget FX); keep blue/purple for real link affordances (`a`, `.site-nav-link`, card-ui link hovers).
- **Links must look like links:** Inline `<a>` in body/card/prose copy must be visibly clickable — accent color + `text-decoration: underline` (+ bold, optional "→" for action links). On the purple bubble theme use `#d946ef`. Never leave a link relying on inherited plain-text styling inside dark card/`border: solid black` wrappers. See `memory/issues.md`.
- **Site chrome:** Main nav’s first item is **Home** (`/`), not “About”; hover title is “Home — bio, intro, and what’s new”. Full Q&A lives on `QA/qa.html` only. Nav **Search** button is hidden; home-page search (`index.html#site-search`) and `/search` remain available. **Quick links (site-wide):** footer-style quick-links carousel (site-map order, drag, arrows, auto-scroll) injected at the top of `.container` via `injectSiteQuickLinks()` in `scripts/components.js`; same component/data as footer carousel. Liquid-glass pill styling; white link text with green hover/active (`--site-nav-glass-*` in `css/owenminercs.css`). No second `.site-nav-pillars` row.
- If the user gives a specific card ratio, component size, layout density, color direction, or interaction rule, add it here.
- **Layout density:** Prefer dense layouts — minimize vertical card padding, section wrapper padding, and header-to-content gaps so more content fits above the fold; keep line-height readable (≈1.28–1.35 on card copy), not crushed. Top-of-page: compact full-width affiliate disclosure **below the page h1**, no “Let’s get started” filler, tight title → disclosure → card grid rhythm on hub pages. **Hub intro pattern (Gear/Gaming reference):** `.intro` → `.gallery.keep-board-intro` with `h1` + optional one-line tagline → `affiliate-disclosure` below title when required → `.hub-content-panel` for main content (long SEO/cross-link blocks go to bottom `.gear-hub__seo` / `.gaming-hub__seo`, not the hero). Programs, For Sale, and Donators use this; inner wrappers (`garage-sale-body`, `donators-body`) live inside `hub-content-panel`. Garage Sale has no top affiliate disclosure (eBay only). **Hub hero density (2026-06):** page `h1` in `.intro` / `.keep-board-intro` uses `clamp(1.4rem, 3vw, 1.8rem)` with minimal margin; intro panel padding/gap tightened; hub card thumbs use `--hub-card-thumb-aspect: 3/2` (vs 4/3 on detail boards); shared `--card-grid-board-padding`, `--card-body-padding-*`, and hub row-gap tokens reduced in `owenminercs.css`.
- **Subpage density / more content per scroll (2026-06):** **Hub pages** (home, Gear hub, Gaming hub, Socials hub, etc.) — current card-grid density is good; do not regress. **Subpages** (detail/content pages linked from hubs — setup tours, gaming memorabilia, merch galleries, etc.) must show **more above the fold**: compact heroes (`.setup-page-compact`, tight lede margins), smaller gallery thumbs, tighter vertical rhythm between sections. **Eye candy = more items visible per viewport**, not giant single images or full-bleed hero photos. Oversized pictures waste scroll and lose visitors — prefer **dense multi-column grids** (`.photogallery.subpage-gallery--dense`: 5/4/3/2 cols like nosmoking wallpaper gallery), hub-thumb density tokens (`--card-grid-*`, `--hub-card-thumb-aspect`), and `setup-page-compact` patterns. First gallery row should be visible with minimal scroll after the title. Scope CSS to `.setup-detail-page` / page classes — do not shrink hub `.keep-board` grids. Reference: `Counter-Strike/nosmoking.html`, `Gaming/gaming-memorabilia.html`.
- **Card grid density (2026-06):** Target **~5 cards per row** on wide desktop in the main `site-card-ui` column (`--site-main-column-max: 1240px`). Shared tokens in `owenminercs.css`: `--card-grid-col-min` (~13.5rem), `--card-grid-col-gap`, `--card-grid-row-gap`, `--card-grid-board-padding`. Default `.keep-board` uses auto-fill; `.keep-board--hub` uses explicit **5/4/3/2** columns (no 1-col phone fallback). Home explore carousel shows **5/4/3/2** visible cards (CSS + `home-explore-carousel.js`). `.site-teaser-grid` is **5/4/3/2**. **Home YT mosaic is an exception** — keep landscape **2-up** and portrait **4-up** on desktop (not hub 5-up density); see ratio note below. Mobile hamburger nav unchanged.
- **Mobile card grid — Instagram discovery (2026-06):** At `max-width: 700px` (hamburger breakpoint), hub/category `.keep-board` / `.keep-board--hub` / `.site-teaser-grid` use a **2-column** visual tile grid with **no 1-col fallback** at 375–390px. Gaps ~2–4px (`--card-grid-col-gap` ~0.125–0.25rem); square thumbs (`--hub-card-thumb-aspect: 1/1`, `object-fit: cover`); compact title below thumb; hide hub card video-slot, affiliate blurb, CTA, and teaser desc on mobile for thumb-forward density. Tighter above-the-fold rhythm: `--site-gutter-x`, intro/disclosure/hero padding, hub-content-panel (bubble). Home explore carousel stays **2-up**; YT landscape rows stay **2-up** on phone. Nav/button touch targets stay ≥~44px (`min-height: 2.75rem` on filter/quick-strip controls). Desktop layout unchanged above 700px.
- **Hub cards — no hover descriptions (2026-06):** Do **not** add hover dropdowns, peek panels, or expand-on-hover description overlays on `.keep-card` / hub cards site-wide. Card face may keep a **short** subtitle (`.keep-card__affiliate` / label); full copy belongs on the linked destination page only. `.keep-card__peek` is hidden globally in CSS. Help Wanted cards keep **line-clamped** description on the card face (no hover popover). Keep lift/glow, thumb carousels, and click-through behavior.
- **Hub cards — one photo, one link (2026-06):** Each `.keep-card` must have **one** thumbnail and **one** destination (`data-href`). Do **not** use `.keep-card__thumb-row` with multiple `<a class="keep-card__thumb-shot">` pointing to different pages. The whole card (and its single thumb) navigates via `data-href` only — no nested thumb links to other routes. Multi-image carousels from `keep-thumbs.js` are OK when every slide is from the **same** destination page (auto-fetched gallery). Use `data-keep-thumb-manual="true"` when the card thumb must stay a fixed static image (e.g. setup archive cards on `previous-setups.html`).
- **Hub cards — placeholder globes sort last (2026-06):** In card grids (`.keep-board`, `.site-teaser-grid`, garage-sale grids), cards whose media is **only** placeholder images (globe JPGs, `owenminercs-logo`, `coming-soon-card`, empty `.keep-card__thumb--empty`, SVG) sort **after** cards with real photos. **Legacy gear (sold/retired products, not setup archives)** also sorts last: active real → active placeholder → legacy real → legacy placeholder. Setup timeline cards (`.keep-card__video-slot` exactly **Archive**, or `data-card-setup-archive="true"`) keep editorial order. Detect legacy via `data-card-legacy="true"`, video-slot starting with **Legacy**, `-legacy.html` href, **Archive →** CTA, or **sold** in label/affiliate. Opt out: `data-card-legacy="false"` or `data-card-sort-skip="true"`. Stable within each group; section headings / `.gaming-card-group` wrappers preserved. Shared logic: `window.owenminercsCarouselFilter` in `scripts/carousel-placeholder-filter.js` (embedded in `components.js`) — `sortAllCardGrids()` on DOMContentLoaded + after `keep-thumbs.js` / `gaming-keep-thumbs.js` gallery upgrades; `sortConfigCardsPlaceholderLast()` for home explore carousel config. Reddit/video embed cards are never treated as placeholder-only.
- **Hub cards — no external/shop links on card face (2026-06):** Hub/category `.keep-card` tiles that link to an internal detail page must **not** include `<a href="https://…">` anchors, store CTAs, or price/shop suffixes (`.b_w_link` “| official store”, “| $…”, etc.) on the card — only the card’s `data-href` destination. Short plain-text subtitles in `.keep-card__affiliate` are OK; Amazon, Wooting, Govee, AliExpress, and other retailer/affiliate links belong on the linked **detail** page only. Reference pattern: `The Setup/the-setup.html`, `Gaming/gaming.html`. Audit/fix helper: `node dev/strip-hub-card-external-links.mjs` (hub `.keep-board` sections in `The Setup/`).
- **Cross-page content:** Do not duplicate full galleries/blocks from other pages. Use `.site-teaser-grid` + `.site-teaser-card` with `[data-site-teaser-carousel]` (`scripts/site-teaser-carousel.js`) — image carousel + short desc + CTA linking to the source page. Preserve all photos in carousels, not inline grids. Setup detail pages use `.setup-page-compact`, `.setup-hero-split` (video + panorama side-by-side on desktop).
- **Socials cloud (`Socials/social-cloud.css` + `Socials/scripts/social-cloud.js`):** published date and the stats line (views / likes / upvotes / comments — whatever `scoreLabel` shows) must stay **on one horizontal row** above the title, separated by a middle dot when both are present. Do not revert to stacking date and stats on separate lines unless the user explicitly changes this.
- **Socials cloud card chrome reveal:** top text chrome (title/date/stats/details) should reveal as an overlay animation that expands up from the media area without shifting card/video layout; keep it hidden when not hovered/focused/pinned.
- **Socials cloud cards:** no rotation — cards stay level at all times; pinning levels the card immediately. No purple pin/move strip; pin via card click, drag to reposition when pinned, resize from corners.
- **Content page social cards:** include cards for every post at/over 100 likes (or upvotes for Reddit) across all linked social platforms whenever post data exists in `Socials/data/`.
- **Footer social bar:** include a static, button-only social profile bar in the footer (no drag/rotate movement controls there). Icons stay on **one horizontal row** at desktop (`flex-wrap: nowrap`; horizontal scroll on very narrow screens, not wrap). Brand band: social + disclosure left; merged `.site-footer-support` (bug + usage) right; narrow globe column (~14%). **Footer quick links:** `.home-pillars--footer` carousel between footer nav and brand band (main nav destinations); auto-scroll when visible via `IntersectionObserver` in `initFooterQuickLinksCarousel()` (pause off-screen, hover/focus, hidden tab; no auto-scroll when `prefers-reduced-motion`). **Hover:** pill fill uses each brand’s tooltip accent (Instagram = multi-stop gradient like the callout); footer is excluded from generic card-ui social hover `!important` grays so those fills apply.
- **Footer vertical rhythm (global `site-card-ui`):** Keep nav, footer quick-links carousel, social/disclosure, globe band, and credits visually tight — one compact top rule + back-to-top, full-width nav row, single brand band (no extra hr between sections), narrow globe grid column (~14%), modest `.site-footer-social-bar` padding for hover tips.
- **Floating social dock:** stays in **`.site-header-dock-cluster`** until the user **manually** drags it; then it uses **`position: fixed`** on `body` (`site-support-dock--placed`) and persists. Reset (or double-click) clears `localStorage`, removes `--placed` / drag state, and re-mounts the dock in the header slot (no default bottom-of-viewport strip). **Rotate/resize:** outer rim; **move:** interior when floating.
- **Ko-fi floating donate button:** keep it draggable anywhere in the viewport and persist its last position across page navigation and future visits.
- Current explicit ratio preferences: home page videos use alternating rows in `.home-yt-mosaic` — **16:9** landscape rows (**2-up** desktop) and **9:16** portrait rows (**4-up** desktop, 2-up below 900px); titles sit below each embed; `aspect-ratio` on `.home-yt-tile__media` enforces exact ratios. Wider row/mosaic gaps than hub card grids — do not inherit `--card-grid-*` density here.
- Donators page support-logo hover notes should reveal below the logos, not follow the mouse cursor.
- **Reading bookmarks** (`components.js` per-word click + `.text-word-glow--bookmark` in `owenminercs.css`): neon green highlight (`#39FF14`-based glow on dark; emerald green tint on light). Drag text selection styled to match.
- **Main nav return helper:** after navigating with a header/footer main-nav pill, show a floating "Back" button on the destination page; clicking should jump back to the previous nested page and restore the saved scroll position.
- **Mobile header order:** on phones, place the social bar at the very top of the page (respecting notch/safe-area inset), then show the site logo below it, then the rest of the page content.
- **Mobile main nav:** at `max-width: 700px`, header nav shows **one wrapped row** by default with hamburger on the right; tap hamburger to expand all rows (tap again / outside / Escape to collapse). Desktop nav unchanged.
- **Site logo:** Header/footer use VP9 WebM with **true alpha** at `/images/logo/owenminercs-logo.webm` (67s segment from source 0:53–2:00; `colorkey=0x000000:0.12:0.08` → `yuva420p`; **480×270, 12 fps, CRF 38, ~2.6 MB**). Static transparent PNG poster/fallback at `/images/owenminercs-logo.png` (**640×360, ~284 KB**). Video autoplays and loops via native `<video autoplay loop muted playsinline>` — no JS scrubbing. CSS shows **either** video or PNG (`site-logo--motion` / `site-logo--still`), never both — do not set `display: block` on `.site-logo.site-logo--footer` (it overrides the motion toggle). **Header logo is left-aligned** in `.site-header-brand-row` (social dock cluster stays right); scales up (`scale(1.35)`, `transform-origin: left bottom`) so foliage bleeds behind sticky nav; nav uses semi-transparent `backdrop-filter` blur. **Footer logo band** (`.site-footer-brand-band`): clipped background layer (`.site-footer-logo-layer`, `overflow: hidden` on band + layer); link/video at `width/height: 100%`, **`object-fit: contain`**, **`transform: scale(1.48)` desktop / `1.4` tablet / `1.42` mobile** (~same visual size — band height tokens unchanged); social + disclosure in `.site-footer-meta__left`, bug report + usage in `.site-footer-meta__right` inside `.site-footer-meta--around-globe` 3-column grid (**~14% center** for globe core; copy may overlap foliage, not rotating globe text); mobile pins logo layer to fixed top height + matching meta `padding-top` so stacked text sits below globe. Both header/footer videos use `preload="metadata"` (poster covers until decode). `prefers-reduced-motion` shows PNG only. Favicons: `/images/logo/favicon.ico`, `favicon-32.png`, `favicon-16.png`, `apple-touch-icon.png` — **globe only, no text**; source frame **0:52** (red globe before orange text ring at ~0:53); auto-detected circular crop + anti-aliased edge; **transparent background on all sizes** (no dark matte); Google-style **~93% fill** (globe nearly edge-to-edge at 16/32); 3D shading = spherical normal lighting + source marble texture recolored to foliage green (highlights **#50fe6a**, mid **#2d8a3e**, shadows **#124826**); apple-touch 180×180. Regenerate: `node dev/generate-favicon-globe.mjs`.

## Content Preferences

- **On-site embeds (2026-07):** In content sections (hub highlights, detail galleries, home video rows), prefer click-to-play YouTube/TikTok embeds, X/Twitter post embeds, Reddit embeds, or links to in-site detail pages — not outbound watch links or social post URLs that send visitors off-site. Footer/social nav profile links and third-party reference/credit links (e.g. mod guides on other channels) may stay external. Bulk migration needs user approval; list audit findings first. Site patterns: iframe `data-embed-src` + embed facade (`components.js`), TikTok player v1 URLs, `keep-card--static` for non-navigating embed tiles on hub boards.
- **X (Twitter) handle:** `@owenminer` — profile URL `https://x.com/OwenMiner` (former handle `@owenminercs` / `OwenMinerCS` is retired).
- **Privacy on archived social screenshots:** The first PC build Instagram PNG (`images/archive/old-pcs/first-pc-build-instagram.png`) must not show readable **usernames or profile photos**—keep them blurred/redacted when replacing the file.
- **Page content usage:** Footer asks visitors to contact on Discord for usage rights for any content on the page; small creators/individuals often get rights free; large companies pay for commercial use (including training AI models).
- Preserve affiliate/support/legal disclosure accuracy.
- **Affiliate links:** Put **(affiliate)** immediately before each Amazon Associates (`tag=owenminercs-20`) or compensated AliExpress link in page copy; keep normal retailer links (Wooting, KBDfans, Govee direct, etc.) unlabeled. Do **not** restore the old JS-generated Amazon/AliExpress shortcut button rows (`data-affiliate-product` / `.affiliate-button` strips).
- **Affiliate link URL format (search, not direct product):** Amazon and AliExpress affiliate links on public pages and in `affiliate-links.json` should use **search** URLs, not direct product/item URLs. **Why:** direct `/dp/` / ASIN and AliExpress `/item/` links break when a listing goes unavailable, delisted, or region-blocked; search links still land shoppers on relevant results. **Amazon (OK):** `https://www.amazon.com/s?k=<query>&tag=owenminercs-20` (plus `+` or `%20` between terms). **Amazon (avoid):** `amazon.com/dp/…`, `amazon.com/gp/product/…`, and slug paths like `amazon.com/…/dp/…` with `tag=owenminercs-20`. **AliExpress (OK):** `https://www.aliexpress.com/wholesale?SearchText=<query>`. **AliExpress (avoid):** `aliexpress.com/item/…` and `aliexpress.us/item/…` for compensated links. **Reference fix:** `The Setup/mouse-pad.html` (Wallhack SP-004 search). **JSON:** prefer `links.amazon_search` or a search string in `links.amazon`; use `links.aliexpress_search` or wholesale search in `links.aliexpress`. **Exceptions (keep as-is unless user says otherwise):** official/direct non-affiliate URLs (`links.official`, `links.direct`, Wooting store, etc.); AliExpress **Portals** deeplinks (`s.click.aliexpress.com/…`); user-approved **store** links such as Perfect World CS2 merch (`pwrdesports.aliexpress.com/store/…` on `Gaming/cs2-merch.html`). **Bulk migration:** do not rewrite live affiliate URLs in HTML/JSON without explicit user approval in chat.
- **Affiliate disclosure (below page title):** Pages with affiliate or referral shopping links need a clear top-of-page note near the intro using class `affiliate-disclosure` with label `Disclosure:` (uppercase green label + colon in `owenminercs.css`; dark readable panel, not liquid-glass inherit). **Placement:** immediately **after the main page `h1`** (title → disclosure → content or one-line tagline). Long SEO/cross-link paragraphs do **not** belong here; see **SEO intro placement**. Back nav (`.setup-detail__back`) stays above the title block. Place inside `.gallery.keep-board-intro` / hero panel when the title lives there (same pattern as `Gaming/gaming.html`). **Layout:** full width of the intro/container column (`max-width: none`), compact padding (~0.42rem × 0.65rem), ~0.8rem body text. **Copy tone:** warm thank-you + FTC clarity (`affiliate-links.json` keys `top`, `topAmazon`, `topAliExpress`). Amazon-heavy pages add “As an Amazon Associate I earn from qualifying purchases.” AliExpress merch: `topAliExpress`. `Garage Sale/garage-sale.html` has no top affiliate disclosure (eBay store link only). **Footer:** shorter page-specific note via `shared-footer disclosure=`; cross-page Amazon byline once in `components.js` (`site-footer-meta__amazon`). Regenerate/reposition top blocks: `node dev/add-affiliate-disclosures.mjs`.
- **Govee — affiliate vs referral (Refr):** On-site “shop this” / product CTAs use **affiliate or approved program links** when enrolled (and terms allow public use). Do **not** pair a **Refr post-purchase referral** link on the same button or competing primary CTA. Reserve Refr/customer referral for contexts that match that program (DMs, post-purchase share, “I bought it—here’s my referral”). Confirm Govee + network + Refr terms before combining channels; disclose whenever compensation applies (extend `lighting.html`-style copy if Govee affiliate or referral credit applies).
- Do not guess real affiliate IDs or private account details.
- Public profile copy should stay channel/content focused. Home-page **About** (`index.html#about`): personal intro + college programming stack (Java, C++, C#, Python, HTML/CSS/JS, SQL, PL/SQL), computer origin story (first teardown 12, first gaming PC 13), hobbies, Counter-Strike fandom, site-as-hobby framing (personal archive + future resource hub, evolves over time), social outreach CTA. Do not add DMACC, degree, age, or alumni copy to the visible About block unless explicitly requested. Do not duplicate the full About block elsewhere without asking.
- Person JSON-LD on production pages should not include `alumniOf` unless there is a new explicit content decision; archived mockups/backups may still contain old bio/DMACC copy.
- Public FAQ scope is intentionally short: height questions and website bug reporting. Livestream discovery, CS configs, and merch links belong in navigation/Socials/Gaming/content pages, not the embedded FAQ.
- **Copy approval (hard rule):** Any visible text the user did not manually write must be **approved in chat before** it lands in the repo. Do not silently add intros, taglines, FAQs, “Also on this site…”, SEO paragraphs, hub ledes, product blurbs, or JSON-LD/marketing copy. **Propose in chat → user says yes → then commit.** If the task is code/CSS/layout only, do not invent copy to fill space. **Allowed without prior approval:** standardized affiliate/disclosure strings already used site-wide; alt text; exact product names/SKUs the user supplied; reformatting or fixing typos in existing user copy.
- **No em dashes (hard rule):** Do **not** use em dashes (`—`, U+2014) or spaced double hyphens (` -- `) in agent-written visible site copy or agent-written prose (memory notes, handoffs, proposed copy in chat). That punctuation is treated as AI slop on OwenMinerCS. **Use instead:** periods, commas, colons, or parentheses. **Scope:** page body copy, titles, ledes, captions, alt text, button labels, meta/OG descriptions, JSON-LD marketing strings, and any new copy agents draft for the user. **Exception:** keep dashes verbatim when the user explicitly wrote that exact dash in approved copy (fix only when they ask). When editing user stories, replace em dashes with acceptable punctuation if the user approved the edit.
- **No hyphenated AI compound labels:** Do not use phrases like `phone-as-camera` or similar dashed noun stacks in visible copy. Say `iPhone`, `iPhone setup`, or `iPhone camera angle` instead.
- **No unsolicited AI copy (examples of what not to add):** “Quick FAQ”, “What is on this page?”, “Also on this site…”, cross-link spam paragraphs, or other SEO filler. Page intros = title + required disclosure + real content only.
- **SEO intro placement (hard rule):** Keyword-stuffed or cross-link SEO paragraphs must **not** sit in the hero above the fold. Keep the hero to `h1` + required `affiliate-disclosure` + (optional) one short visitor-facing tagline. Move long SEO/descriptive intro blocks to the **bottom** of the page (inside or just after `hub-content-panel`), still in the DOM for crawlers, de-emphasized styling (`.gear-hub__seo`, `.gaming-hub__seo`, or equivalent). Reference: `The Setup/the-setup.html`, `Gaming/gaming.html`.
- **Setup Tools page** (`The Setup/tools.html`): physical tools only—links to the cordless drill page and iFixit write-up. PGL Shanghai Major merch and other CS2 content stay on `Gaming/gaming.html`; outfit fit checks live under **Outfits** (`The Setup/clothing.html`).
- For CS2 Perfect World merch callouts on `Gaming/cs2-merch.html`, use the user's preferred source link: `https://pwrdesports.aliexpress.com/store/1103775565?spm=a2g0o.store_pc_allItems_or_groupList.pcShopHead_2009118762807.0`.
- **Merch spin viewers:** Interactive turntable drag-to-scrub loops on `Gaming/cs2-merch.html`. Full workflow (encode, trim, HTML, multi-instance) in `memory/spin-viewer.md`.
- Coaching is deferred for later; do not show or add coaching language on public site pages unless the user asks to restore it.
- Shop preference: PayPal first for small physical drops; Stripe Payment Links are acceptable when card-first checkout, custom fields, inventory limits, or tax handling matter.
- For sale / listings: prefer **in-house direct checkout** (PayPal, Stripe, etc.) when linked on an item; keep **eBay as optional** for the same inventory so buyers can compare — user wants to avoid eBay’s fee-heavy middleman when possible.
- Shop roadmap: signed sticker packs, signed photography/art prints with optional short notes, and future custom sewing projects (clothing, pillows, handmade pieces) marked TBD until ready.
- Shop fulfillment: stickers and prints can be printed at home and self-shipped via USPS/UPS; prefer prices that include shipping, packaging, materials, and payment fees.
- Shop shipping defaults: stickers use the cheapest USPS stamped envelope route by default with no tracking; prints use protective rigid mailers with USPS Ground Advantage tracking. UPS is a backup by request.
