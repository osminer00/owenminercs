# Issues And Diagnostics

Last reviewed: 2026-07-09

Use this file for active bugs, recurring failures, and diagnosis patterns future chats should remember.

## Active Issues

- Markdown/memory docs have encoding/mojibake in several files. See `MARKDOWN_AUDIT.md`.
- Agent memory is being consolidated around `AGENTS.md` and `memory/`.
- **Socials content cloud — drag hint + Shorts drag (2026-06-29):** `mountDragHint` must not gate on `socialCardPinMoveProgress.moved` (achievement progress predates hint; blocked first-time hint). Hint z-index ≥ filter bar (`17`); remount after delayed `syncCloudBounds`. Card `pointerdown` must allow `.smc-player` / iframe surfaces before `isInteractiveCardTarget` — inline YouTube Shorts iframes cover most of the card; `deferPointerCapture` only runs when pointerdown is not short-circuited on `iframe`.
- **Socials mobile footer (2026-06-30):** Footer was ~450px on phone because globe meta padding + desktop-only social-tip bar padding applied at all widths, and in-flow footer sat over the fixed cloud. Fix: `Socials/social-cloud.css` — compact mobile footer (fixed bottom, hide bug/usage copy, smaller globe); scope large tip/globe padding to `min-width: 701px`. Global `owenminercs.css` `@700px`: hide duplicate footer nav/back-to-top, tighten globe band on all `site-card-ui` pages.
- **Socials content cloud — cards/layout/data (2026-06-30):** Card chrome (title, date, stats) always visible; no bottom Link/channel buttons. Full-viewport scatter with header/footer bleed; density-based card count; overlap nudge in `layoutCloudScatter`. Large card/video aspect CSS (16:9 / 9:16). Local JSON feeds skip global 101-like filter; YouTube 101 likes or 5k views; Reddit from `Socials/data/reddit-posts.json` when present. Live Reddit fetch skipped on localhost. **TikTok thumbs:** signed `tiktokcdn` URLs 403 in browser — cache to `Socials/images/content-thumbs/tiktok/{id}.jpg` via `node dev/cache-social-thumbnails.mjs` (run after `merge-tiktok-recent.mjs`). Cloud JS uses local paths + never requests expired CDN URLs. Drifting cards use thumbnails only; embeds on pin. **Reddit image cards (2026-07-10):** `getEmbedConfig` only returned MP4 `<video>` for `contentType: video`, so image/post entries in `reddit-posts.json` (`embedUrl: ""`) had no embed on pin — thumbnail only, bottom Link chrome hidden. Fix: fall back to `redditPostUrlToMediaEmbed` → `redditmedia.com` iframe for all Reddit cards (MP4 video when available, else iframe); pinned slot min-height ~280px in `social-cloud.css`.
- **Socials view-all-content sync (2026-07-10):** `view-all-content.js` now loads top **50 by view count** for YouTube (deduped shorts + long-form), TikTok, and Instagram, plus all Reddit posts with **100+ upvotes**. Replaces the old 101-like / 5k-view engagement gate that left ~48 cards. YouTube refresh: `node scripts/sync-youtube-local-feed.mjs`. TikTok thumbs: `node dev/cache-social-thumbnails.mjs`. Reddit: `dev/sync-reddit-posts.mjs` — `submitted.json` often 403; oembed-verified seed list (10 posts). Instagram: `scripts/sync-instagram-posts.mjs` — yt-dlp extractor broken; added `web_profile_info` API fallback (may 429 without login). Manual `instagram-posts.json` still OK.

## Recurring Patterns

- **Lite visuals / HW accel off (2026-07-12):** Chrome with “Use graphics acceleration when available” off still runs `backdrop-filter`, bubble video, and starfield canvas, but compositing falls back to CPU — scroll jank on bubble/home pages. Fix: `html[data-low-effects]` via `scripts/low-effects.js` (frame-budget auto-detect + footer toggle + `localStorage` `owenminercs-low-effects`: `on`|`off`|auto). CSS: `css/low-effects.css`. Disables backdrop blur, bubble video (poster stays), starfield inject, bubble-scroll rAF, dock turbo/ice-coast. Repeat visits with stored `on` bootstrap from top of `components.js`. Hub/product `.keep-card[data-href]` cards were `<div role="link">` with JS `click` navigation only, so right-click showed generic browser menu (no Open link in new tab). Fix: `initKeepCardPrimaryLinks()` in `scripts/components.js` injects `<a class="keep-card__primary" href="...">` overlay; `css/owenminercs.css` uses pointer-events passthrough on `.keep-card__inner` descendants so the anchor receives clicks/right-clicks. Interactive children (affiliate links, album nav, swipe viewport, embeds) keep `pointer-events: auto`. Swipe-hub `.keep-card__album-viewport` still blocks link context menu on the thumb strip (drag target); left-click there still navigates via fallback handler. Site-map cards (`.site-map-card`) excluded (Ctrl/Shift modifiers).
- **Keep-card body not clickable (2026-07-12):** Gear/Gaming hub cards looked dead on card body clicks. `initKeepCardPrimaryLinks()` only registered on `DOMContentLoaded` when `readyState === 'loading'`, but `defer` on `components.js` usually runs at `interactive`, so init never ran (0 `.keep-card__primary` overlays). Fallback click handler only navigated on `.keep-card__album-viewport`. Fix: call `initKeepCardPrimaryLinks(document)` in the `else` branch; navigate on any non-interactive card click; inline `position:absolute; inset:0; z-index:1` on injected primary link; `position:absolute !important` in CSS. **Home Explore carousel title/desc dead (2026-07-12):** `.home-explore-card__primary` only wrapped the thumbnail `<a>`, not the title/desc block. Fix: empty full-card overlay `<a class="home-explore-card__primary">` as first child; media + body as siblings with pointer-events passthrough; nested `.home-explore-card__links a` stay `pointer-events:auto`; article click fallback in `home-explore-carousel.js`. **Inline links look like plain text (2026-07-12):** Anchors in dark bubble `.hub-content-panel` / `.intro` prose inherited body text color with no underline. Fix: global rules in `css/owenminercs.css` (green `#4ade80` unvisited, purple `#d946ef` visited, underline, bold) for bubble-theme content panels; `pc-part-name-link` accent on bubble PC build pages; `labelAffiliateLinkTitles()` in `components.js` adds `title="Amazon affiliate"` / `title="AliExpress affiliate"` when missing. Page-specific overrides OK (e.g. `Keyboard/60he-2023.css` cyan part rows).
- **Home carousel drag (2026-06-30):** Explore + Top performing clips carousels — native `<img>` drag (prohibited cursor) breaks pointer drag; set `draggable="false"`, `-webkit-user-drag:none`, capture-phase `dragstart` prevent on viewport. YT rows: `pointer-events:none` on carousel iframes (play via facade button only); align YT drag with explore (`pointerActive` + document `pointermove`/`pointerup`, threshold before capture).
- **Home carousel hold-pause (2026-07-04):** Explore + Top performing clips — `interactionPaused` on `pointerdown` stops autoplay; `tryResumeAuto()` on `pointerup` resumes when not hovering/focusing/dragging. 6px drag threshold, viewport pointer capture, click suppression when `didDrag`. YT rows gained autoplay (`INITIAL_AUTO_MS`/`AUTO_MS`) matching explore; `prefers-reduced-motion` skips autoplay, drag still works. **Memorabilia photo carousels (2026-07-10):** drag (threshold crossed) or prev/next button press/hold sets `userPauseUntil = now + 5min`; autoplay stays off until that expires (hover/focus pause still applies but does not override the 5-min window). See `scripts/memorabilia-photo-carousel.js`.
- **Infinite transform carousels (2026-07-10):** Clone count must cover the full visible viewport. Animate navigation into boundary clones, then reset to the equivalent original position only after `transitionend`; forcing modulo-normalization before a drag settles creates a visible full-track jump. During unbounded pointer drag, silently rebase the transform by one original-track width at equivalent clone boundaries. See `scripts/memorabilia-photo-carousel.js`. **When `cloneCount === 0`** (slide count ≤ visible tiles, e.g. 2-slide `#bf-poster` / `#black-ops-shrine`), skip clone-loop `syncNormalize()` — treating `slideIndex <= 0` as a boundary rewrote index `0` to `originalCount` and translated the track off-screen (blank carousel). Fixed 2026-07-10 in `memorabilia-photo-carousel.js`.
- **Footer quick-links carousel drag (2026-07-04):** Footer pill carousel had autoplay + native `overflow-x` scroll only — no pointer drag handlers, so link anchors captured drags and navigation fired on release. Fix: `bindFooterQuickLinksDrag()` in `components.js` (6px threshold, document `pointermove`/`pointerup`, `scrollLeft` drag, capture-phase click block when `didDrag`; pause autoplay on drag). CSS: `cursor: grab` / `--dragging` + `pointer-events: none` on pills during drag. Shared via `<shared-footer>` on all `site-card-ui` pages.
- **Home Reddit carousel cut off (2026-06-30):** Reddit `widgets.js` iframes have a wide min-width; carousel tile `flex-basis: calc(100% / n)` resolved against the expanding flex track, so the right tile clipped the viewport. Fix: `container-type: inline-size` on `.home-yt-carousel__viewport` + tile width via `100cqw`; Reddit tiles use fixed `--reddit-embed-height: 400px` (not 16:9) + iframe `max-width: 100%` like `.keep-card__reddit-embed`. Match `data-embed-height` on `.reddit-embed-bq` to the CSS var; do not cap with `max-height` or tall image posts clip vertically.
- **Home Reddit/X carousel engagement (2026-07-12):** Home mosaic X + Reddit rows require **100+ likes/upvotes** (`MIN_SOCIAL_LIKES` in `dev/sync-home-top-clips.mjs`). Regenerate X data: `python scripts/sync-x-top-posts.py` (or `node dev/discover-x-posts.mjs`) then `node dev/sync-home-top-clips.mjs`. Reddit: `node dev/sync-reddit-posts.mjs` then `node dev/sync-home-top-clips.mjs`. **X sync (2026-07-13):** Nitter RSS is down; `scripts/sync-x-top-posts.py` scrapes status IDs from `zamantika.com/profile/owenminer`, enriches via fxtwitter, keeps **replies + text-only** posts at 100+ likes (not media-only). **Home Reddit row (2026-07-12):** click-to-load `redditmedia.com` iframe facades + cached thumbs; do **not** load `embed.reddit.com/widgets.js` on home. When pullpush/submitted.json fail, home merges oembed-verified seed posts with local thumbs (10 curated). TikTok row: `Socials/data/tiktok-posts.json` at 100+ likes, `player/v1` + facade.
- `initWordBackgroundGlow` wraps words in `<span class="text-word-glow">`. Do not use `display: flex; flex-direction: column` on those containers — each word becomes a flex item and stacks one word per line. Use normal block flow, or `flex-direction: row` with `flex-wrap: wrap` (see `Achievements/achievements.html` `.achievement-card__hint`).
- **Main column width — hero vs body panels:** `.intro` used default `content-box` with `width:100%`, so padding inflated hero glass wider than `border-box` `.hub-content-panel` (and home `.home-yt-section` lacked home padding token). Fix in `css/owenminercs.css`: `body.site-card-ui .container { padding-inline:0 }` + direct-child panels `box-sizing:border-box; width:100%`.
- Floating `#site-support-dock` default position must not use header/logo rects: pages differ (`body` `zoom`, scrollbars) and the bar would shift between routes. Default is fixed viewport `(2px, 2px)` after clamp (see `getSocialDockDefaultViewportPosition` in `scripts/components.js`).
- Inline `zoom: 95%` on `body`: do **not** inverse-zoom only `shared-header` / `shared-footer` — that mis-centered `.container` vs the sticky header (nested zoom). Header/footer/main now share body zoom; home/Socials without body zoom stay unchanged.
- `body` sets `justify-content: center` with `min-height: 100vh`; `body.site-card-ui` must override with `justify-content: flex-start` and give the main column (`> .container`, `> main`, `> .calc-container`) `flex: 1 1 auto` so short pages do not vertically center header/content/footer on tall viewports.
- Worktree is often heavily modified before agent work starts. Always preserve unrelated changes.
- **TikTok player/v1 embeds — Access Denied:** TikTok rejects iframe loads when the parent sends no referrer. Site `_headers` and iframe defaults use `Referrer-Policy: strict-origin-when-cross-origin`, which strips referrer on `http://localhost` → `https://tiktok.com`. Fix: set TikTok iframes to `referrerpolicy="origin-when-cross-origin"`; in Socials cloud show TikTok thumbnails until pin (load player on click). See `getTikTokEmbedUrl` / `shouldUseInlineTikTokPlayer` in `Socials/scripts/social-cloud.js` and `applyTikTokIframeReferrerPolicy` in `scripts/components.js`.
- Some docs and handoffs are stale or duplicated; verify current source files before trusting old handoffs.
- **Bubble-theme pages — dynamic content outside intro glass:** On bubble-theme hub pages, only `.intro` (or `.garage-sale-page .intro#shop-intro`) gets liquid-glass by default. Listing grids, summary blocks, and other JS-filled content placed as siblings below `.intro` look disconnected (correct DOM in `.container`, wrong visual column). Fix: nest dynamic content inside `.intro` in a `*-body` wrapper (e.g. `donators-body`, `garage-sale-body`) and style in `css/bubble-themes/{section}.css` — see `Donators/donators.html` + `donators.css`, `Garage Sale/garage-sale.html` + `garage-sale.css`. Remove legacy `.gallery` inline black-border wrappers on bubble pages.
- **Bubble-theme hub pages — intro vs main column disconnect:** After `apply-bubble-themes.mjs`, some hubs had main listings visually outside the intro glass column. Fix: keep **all** hub content inside `body > .container`; hero/disclosure stays in `.intro`; wrap post-intro listings (`.keep-board`, filters, dynamic grids) in shared `.hub-content-panel` (liquid glass in `css/liquid-glass-surfaces.css`) — same pattern as `.help-wanted-section`, `.qa-section`, etc. **Do not** nest `.keep-board` inside `.intro` on category/hub pages; cards stay in their own glass panel below the hero. Donators/Garage Sale/Help Wanted/Q&A/Programs/Achievements use their section-specific wrappers; gear/gaming/socials hubs use `.hub-content-panel`. **Audit 2026-06-29:** fixed Gear hub (`the-setup.html`), Gaming hub (`gaming.html`), Upgrades, 26 Setup category pages, `Gaming/cs2-merch.html`, `The Setup/previous-setups.html`, `Socials/view-all-content.html`. Detail pages with single `.intro` or `.intro2`/`.photogallery`/`.setup-archive-detail` (already in glass selectors) left unchanged. Regenerate batch wrap: `node dev/fix-hub-content-panel.mjs`; audit: `node dev/audit-bubble-hub-panels.mjs`.
- Local machine config file `.claude/settings.local.json` was once tracked; keep it gitignored and untracked to avoid leaking local command permissions or environment-related references.
- Affiliate widgets can over-generate marketplace search buttons from product names; for products without reliable marketplace listings, explicitly disable marketplace buttons and keep official/direct buy links.
- **Affiliate quick-link thumbs (2026-06-30):** `dev/generate-affiliate-quick-links.mjs` used to take the first `.photogallery` image from a detail page — wrong for multi-product pages (`gaming-pc.html`, `PC/pc.html`, `Desk Setup/setup.html`, `camera.html`, `audio.html`). Fix: `resolveProductThumb()` scans all `product.pages` with ASIN/affiliate-link proximity, section headings, keep-card labels, keyword/alt matching; blocks shared heroes (e.g. `pc.webp`) and cross-product paths (`streamerx` on mic-arm items); fallback `/images/logo/globes/globe-blue.jpg`. Regenerate: `node dev/generate-affiliate-quick-links.mjs`.
- Paths are case-sensitive on production-style static hosting. When adding shared nav/sitemap/canonical URLs, match the actual directory casing or add redirects for any previously published casing.
- Social dock edge rotate: `isPointerOnSocialBarEdge` must use layout center/size (`offsetLeft`/`offsetWidth`), not `getBoundingClientRect()` on `.site-social-nav__main` — counter-rotated icons skew the screen bbox after the first spin. Map pointer into pivot-local space with `screenPointToSpinLocal`. Axis-aligned bbox alone also shrinks edge hits on a rotated bar.
- Social dock first placement: `#site-support-dock.site-support-dock--placed` used to inherit column stack from the floating-pill defaults — keep header-style row layout in CSS and apply `site-support-dock--layout-horizontal` before clearing `site-support-dock--drag-lock-horizontal` so the bar does not flash/snap vertical on release.
- **Embed facade two-click play:** Deferred iframes (`data-embed-src` → facade poster) used to set bare embed URL on click — iframe loaded but YouTube/TikTok waited for a second in-frame click. Fix: `activateEmbedFacade` → `loadDeferredEmbed({ autoplay: true })` appends `autoplay=1` (+ YouTube `enablejsapi=1` / `cueYouTubeIframePlay` on load). Intersection-observer lazy loads (carousel layers) stay non-autoplay. See `getAutoplayEmbedUrl` / `loadDeferredEmbed` in `scripts/components.js`.
- **cs2-merch native video controls (2026-07-10):** Phone clips in `.merch-video-grid` loaded (`readyState` 4) but play button / timeline did not respond. Cause: `.hub-content-panel` `backdrop-filter` on `Gaming/cs2-merch.html` breaks UA `<video controls>` hit-testing (`.merch-section--video` alone was not enough). Fix: `css/liquid-glass-surfaces.css` disables panel backdrop-filter on `.cs2-merch-page`; `scripts/cs2-merch-videos.js` click fallback calls `play()` when a click leaves the clip paused. Media lives in `images/cs2-merch/apartment-setup-2025/` (convert from Google Drive via `dev/convert-content-batch-7-16-supplement.mjs`).
- **Carousel placeholder logos:** Hub/explore/teaser carousels used to mix globe/logo fallbacks with real photos when fetching gallery thumbs from target pages. Fix: `window.owenminercsCarouselFilter` in `scripts/carousel-placeholder-filter.js` (also embedded in `scripts/components.js`) — `filterCarouselSlides` / `pruneCarouselImageNodes` drop `/images/logo/globes/*`, `owenminercs-logo*`, coming-soon, favicon, data URLs, and SVG when any real image exists; keep placeholder only if it is the sole slide. Wired in `keep-thumbs.js`, `home-explore-carousel.js`, `site-teaser-carousel.js`, `gaming-keep-thumbs.js`. **Card grid order (2026-06):** same module also exposes `sortAllCardGrids` / `isCardPlaceholderOnly` / `isCardLegacyGear` — active cards first, legacy gear (sold/retired, not setup **Archive** timeline cards) last, placeholder-only cards after active within each tier; runs on load + after thumb upgrades.
- **keep-thumbs.js same-page `#fragment` hrefs (2026-06-30):** When a `.keep-card[data-href="#section"]` points at the current page, `extractGallerySlides` also collects `.keep-board img.keep-card__thumb` from the fetched HTML — sibling hub cards pollute the carousel (e.g. `The Setup/clothing.html` shoes card cycling Carhart/TikTok thumbs). Fix: `data-keep-thumb-manual="true"` on same-page anchor cards, or use a dedicated detail URL without other `.keep-board` thumbs in the DOM. **Not** caused by `keep-thumbs.js` auto-fetch (it skips `data-keep-thumb-manual` cards and only replaces `.keep-card__thumb`, not thumb rows). Fix markup to a single `.keep-card__thumb`; see `memory/preferences.md` hub-card rule. Removed dual-thumb October 2025 / 2024 cards on `The Setup/previous-setups.html` 2026-06-29.
- **Gaming PC gallery2 row squeeze (2026-06-30):** `The Setup/gaming-pc.html` puts hero img + two photo sections + `.case2` inside `.gallery2`, which defaults to `flex-direction: row` (image + parts only). Four flex children collapse into narrow vertical strips. Fix: `body.site-card-ui.setup-detail-page .container > .gallery2 { flex-direction: column; align-items: stretch; }` plus full-width img/case2 caps in `owenminercs.css`. Legacy `.photogallery` defaults to 2 tall columns — wastes scroll on detail pages. Fix: add `.subpage-gallery--dense` (5/4/3/2 grid). Batch: `node dev/apply-dense-galleries.mjs`. **Fixed:** all Gaming detail galleries flagged above + 16 `The Setup/*.html` + `PC/pc.html` + `Keyboard/60he-2023.html` + `60he-2025.html`. **Maybe later:** `Gaming/counter-strike-background.html` (single hero). **Stickers (2026-07-03):** bottom Agent K section nested `keep-board--hub` (5-col) inside `stickers-agent-k-callout` (52rem 2-col) — single card ~20% of narrow column; fixed with `setup-detail-lede-wrap` + single-col `.stickers-agent-k-link` board in `Gaming/stickers.html`.
- **Legacy `.bd4` side-by-side void on gear detail pages (2026-06-30):** Old `.ultrawide` + `.bd4` flex put hero image left and copy in a narrow `.column2` right column with empty space below. Fix: scoped CSS in `owenminercs.css` on `.setup-detail-page` stacks `.bd4` vertically, left-aligns copy, hides `.minimize5` on bubble pages. Disclosure batch: `node dev/fix-setup-detail-disclosure-order.mjs` (title → disclosure). List-heavy example restructure: `The Setup/lighting.html` → `.setup-detail-sections` 2-col grid in `.hub-content-panel`. Category sub-hub inline black borders: `node dev/strip-category-hub-inline-borders.mjs` (excludes `the-setup.html`).
- **Setup archive pages — nested blue/cyan glass wash (2026-06-30):** `.setup-archive-detail` inside `.intro.setup-detail__content` got a second liquid-glass stack (`::after` chromatic glow uses gear theme cyan). Text and photos looked covered by a blue gradient (`legacy-laptop.html`, `upgrade-desktop-2020.html`, other archive detail pages). Fix: drop `.setup-archive-detail` from global glass surface selectors in `css/liquid-glass-surfaces.css`; neutralize nested glass on `.intro .setup-archive-detail` (same pattern as `.photogallery`).
- **Gaming memorabilia carousel stage squashed (2026-06-30):** `.memorabilia-box-carousel` used `grid-template-rows: auto minmax(0, 1fr)` with `__main` + `__thumbs` both in grid-area `media` (thumb strip overlays stage). With no definite carousel height, the `1fr` row min was 0 and the hero stage collapsed to a thin sliver; thumbs kept their fixed size. Fix: `grid-template-rows: auto auto` in `css/bubble-themes/gaming.css` so the media row sizes from the stage `aspect-ratio` / `max-height`. **2026-07-10:** `Gaming/gaming-memorabilia.html` moved to `bubble-theme--socials` and replaced hero+thumb-strip `.memorabilia-box-carousel` with home-style drag carousels (`.memorabilia-photo-carousel` + `scripts/memorabilia-photo-carousel.js`, CSS in `css/owenminercs.css` scoped to `.gaming-memorabilia-page`). **2026-07-10 (later):** memorabilia carousels stacked one per row (`.memorabilia-carousel-grid` → single column); 4-up edge-to-edge tiles (`gap: 0`, `flex: 0 0 25cqw`, 2-up at ≤900px) matching home YT row pattern; `getVisibleCount()` in `memorabilia-photo-carousel.js` drives clone count + scrollability., `.hub-content-panel::after` (bottom 50% chromatic glow via `--lg-glass-glow-mid`; gaming theme = red/maroon) painted **on top of** nested content when direct children lacked `z-index` above the pseudo (e.g. `Gaming/gaming-memorabilia.html` Skyrim grid at panel bottom). `.photogallery` also had the same `::before`/`::after` glass stack. Fix in `css/liquid-glass-surfaces.css`: disable `::before`/`::after` + transparent fill on `.photogallery`; add `z-index: 1` on `.hub-content-panel > *` and `.setup-archive-detail > *` (and include `.hub-content-panel` in the content-above-pseudo lift rule). Gear archive pages (e.g. `The Setup/legacy-laptop.html`) read cyan/blue when gear `--bubble-glow-cool` bleeds through. Hub panel edge glass stays; photo pixels stay full clarity per `memory/preferences.md` no-image-overlay rule.
- **keep-thumbs album thumb click (2026-06-30):** Cards with 2+ gallery images on the target page get a cycling `.keep-card__album` thumb. A full-size `.keep-card__album-pause-hit` overlay intercepted clicks (`stopPropagation`) and `setup-cards.js` skipped navigation on that target — thumb clicks did nothing useful (only toggled pause). Fix: remove pause-hit overlay from `keep-thumbs.js` / `gaming-keep-thumbs.js`; thumb clicks bubble to card navigation. Hover/focus still pauses slideshows via `bindAlbumCarouselPause`. `scripts/site-map-page.js` `pageHref()` omitted the `.html` suffix Live Server needs (unlike `getLink` / `resolveSiteSearchHref` in `components.js`). Cards wired via `wireKeepCards()` navigated to extensionless paths → 404. Fix: delegate to `window.owenminercsSiteSearchApi.resolveHref` when available.
- **Browser back scroll restoration (2026-07-11):** Back/forward landed at scroll top because only header `.site-nav-link` clicks + the custom floating Back button saved scroll (`NAV_RETURN_*` in localStorage); keep-cards and body links did not. Post-load DOM reorder (`initCardGridPlaceholderSort`, async `keep-thumbs.js`) can also invalidate native restoration. Fix: `initSiteScrollRestoration()` in `scripts/components.js` — `history.scrollRestoration = 'auto'`, persist `scrollX`/`scrollY` per URL in `sessionStorage` on scroll/pagehide/same-origin link click, restore on `pageshow` (`persisted` or `navigation.type === 'back_forward'`) after DOM settles (double `rAF`). Skips hash targets (except `#top`) and defers to existing `NAV_RETURN_SCROLL_KEY` restore.
- **Hub back link scroll restoration (2026-07-11):** `← Gear` / `← Gaming` / `← Content hub` links (`.setup-detail__back`, `.vac-back`, optional `.hub-back-link`) are forward navigations (`navigation.type === 'navigate'`), so session restore gated on `back_forward` never ran. Fix: on hub-back link click, read destination URL scroll from `sessionStorage` (`SCROLL_POSITIONS_KEY`) and queue `NAV_RETURN_SCROLL_KEY` restore (same path as floating Back button). `applyPendingNavReturnScrollRestore()` now uses `restoreScrollPositionWithRetries()` for DOM-settle retries. Header nav to a hub still starts at top unless floating Back or a prior hub visit saved a position. **2026-07-12:** Hub back still linked to fixed parent hub (`the-setup.html`, `gaming.html`, etc.) so Home → detail → `← Gear` opened Gear instead of Home. Fix: `tryHubBackViaHistory()` in `components.js` — first link in `.setup-detail__back` / `.vac-back` uses `history.back()` when `history.length > 1` and same-origin `document.referrer`; label becomes `← Back`; fallback href unchanged when no history (direct open / external referrer).

## Issue Template

```md
### YYYY-MM-DD - Short title

- Symptom:
- Affected files/pages:
- Suspected cause:
- Fix attempted:
- Verification:
- Status: active | fixed | needs follow-up
```

