# Memory Updates

## 2026-07-27

- Documented client-side site search (`SITE_SEARCH_RUNBOOK.md`) and Steam CS2 inventory API caps (`STEAM_CS2_INVENTORY_RUNBOOK.md`).
- Updated Twitch donator handoff for authenticated EventSub registration (`x-twitch-register-secret` / Bearer).
- Recorded main-nav label **Gaming Setups** (stable `data-nav="The Setup"`) and `@OwenMiner` X handle in project state.

## 2026-04-28

- Added `AGENTS.md` as the canonical cross-platform entry point.
- Added shared `memory/` folder for project state, preferences, issues, decisions, and updates.
- Recorded the user's preference for a cross-platform, token-light, self-updating memory system.
